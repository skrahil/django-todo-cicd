    version: 2.1
    jobs:
    
#Job1
      Version_Check:
        docker:
          - image: cimg/python:3.11.3
        steps:
          - checkout
          - run: python --version
          
          
          
#Job2 
      Checking_Docker:
        docker:
           - image: cimg/python:3.11.3
        steps:
           - checkout
           - run: docker --version
           


#Job3 
      Pulling_Docker_Hello_World:
        docker:
           - image: cimg/python:3.11.3
        steps:
           - checkout
           - run: docker --version
           - run: service docker start
                
           

                    
        

#Workflows     
    workflows:
      Test-Deploy:
        jobs:
            - Version_Check
            - Checking_Docker:
                requires:
                    - Version_Check 
            - Pulling_Docker_Hello_World:
                requires:
                    - Checking_Docker
            
          
       
