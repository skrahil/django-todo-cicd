name: Python Application

on:
  push:
    branches: [ develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: code checkout
    - uses: actions/checkout@v1
      run: |
        echo "Successfully GitHub Action"
